{
  "name": "copilot-lmapi-bridge",
  "displayName": "Copilot-LMAPI",
  "description": "Bridge VS Code Copilot Language Model API to OpenAI-compatible HTTP API",
  "version": "0.3.3",
  "publisher": "copilot-bridge",
  "engines": {
    "vscode": "^1.92.0"
  },
  "categories": [
    "Other"
  ],
  "keywords": [
    "copilot",
    "lmapi",
    "api",
    "bridge",
    "language model"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "copilot-lmapi.start",
        "title": "Start LM API Server",
        "category": "Copilot-LMAPI"
      },
      {
        "command": "copilot-lmapi.stop",
        "title": "Stop LM API Server", 
        "category": "Copilot-LMAPI"
      },
      {
        "command": "copilot-lmapi.restart",
        "title": "Restart LM API Server",
        "category": "Copilot-LMAPI"
      },
      {
        "command": "copilot-lmapi.status",
        "title": "Show Server Status",
        "category": "Copilot-LMAPI"
      }
    ],
    "configuration": {
      "title": "Copilot-LMAPI",
      "properties": {
        "copilot-lmapi.port": {
          "type": "number",
          "default": 8001,
          "minimum": 1024,
          "maximum": 65535,
          "description": "HTTP server port (requires restart)"
        },
        "copilot-lmapi.host": {
          "type": "string",
          "default": "127.0.0.1",
          "description": "HTTP server host (127.0.0.1 for localhost only)"
        },
        "copilot-lmapi.autoStart": {
          "type": "boolean",
          "default": false,
          "description": "Automatically start server when VS Code starts"
        },
        "copilot-lmapi.enableLogging": {
          "type": "boolean",
          "default": true,
          "description": "Enable detailed logging"
        },
        "copilot-lmapi.maxConcurrentRequests": {
          "type": "number",
          "default": 10,
          "minimum": 1,
          "maximum": 100,
          "description": "Maximum concurrent requests"
        },
        "copilot-lmapi.requestTimeout": {
          "type": "number",
          "default": 120000,
          "minimum": 5000,
          "maximum": 600000,
          "description": "Request timeout in milliseconds"
        },
        "copilot-lmapi.modelCacheRefreshInterval": {
          "type": "number",
          "default": 300000,
          "minimum": 60000,
          "maximum": 3600000,
          "description": "Model cache refresh interval in milliseconds (default: 5 minutes)"
        },
        "copilot-lmapi.modelHealthCheckInterval": {
          "type": "number", 
          "default": 600000,
          "minimum": 60000,
          "maximum": 3600000,
          "description": "Model health check interval in milliseconds (default: 10 minutes)"
        },
        "copilot-lmapi.allowRemoteImageDownload": {
          "type": "boolean",
          "default": false,
          "description": "Allow the bridge to download remote images (http/https) and include them as LanguageModelDataPart. Disabled by default for security."
        },
        "copilot-lmapi.maxImageBytes": {
          "type": "number",
          "default": 20971520,
          "minimum": 1024,
          "maximum": 104857600,
          "description": "Maximum allowed image size in bytes for automatic downloads (default: 20MB)"
        },
        "copilot-lmapi.imageFetchTimeoutMs": {
          "type": "number",
          "default": 5000,
          "minimum": 1000,
          "maximum": 60000,
          "description": "Timeout in milliseconds for fetching remote images (default: 5000ms)"
        },
        "copilot-lmapi.allowedImageHosts": {
          "type": "array",
          "items": { "type": "string" },
          "default": [],
          "description": "Optional whitelist of hostnames allowed for remote image downloads. Empty = allow any host when downloads are enabled."
        }
      }
    },
    "menus": {
      "commandPalette": [
        {
          "command": "copilot-lmapi.start"
        },
        {
          "command": "copilot-lmapi.stop"
        },
        {
          "command": "copilot-lmapi.restart"
        },
        {
          "command": "copilot-lmapi.status"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src --ext ts",
    "test": "node ./out/test/runTest.js",
    "package": "vsce package"
  },
  "devDependencies": {
    "@types/vscode": "^1.92.0",
    "@types/node": "18.x",
    "@typescript-eslint/eslint-plugin": "^6.4.1",
    "@typescript-eslint/parser": "^6.4.1",
  "eslint": "^8.47.0",
  "typescript": "~5.3.3",
    "vsce": "^2.15.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/BlueSkyXN/Copilot-LMAPI"
  },
  "bugs": {
    "url": "https://github.com/BlueSkyXN/Copilot-LMAPI/issues"
  },
  "homepage": "https://github.com/BlueSkyXN/Copilot-LMAPI#readme",
  "license": "GPL-3.0"
}